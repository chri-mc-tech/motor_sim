#pragma once
#include <raylib.h>

namespace graphics {
    inline Camera3D camera;
    inline int window_width = 1280;
    inline int window_height = 720;
}

namespace global {
  inline bool running;
}